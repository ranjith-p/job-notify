# =============================================================================
# Job alert search configuration
#
# Edit this file to customize what the bot searches for and filters out —
# no Python knowledge needed. Lines starting with # are comments and are
# ignored. Blank lines are ignored too. Each setting lives under its own
# [SECTION] header; add or remove lines under a section to change it.
#
# After editing, commit and push this file — the next run picks it up
# automatically.
# =============================================================================

# ---------------------------------------------------------------------------
# KEYWORDS — job title/description phrases to search for (one per line).
# Each is queried as an exact phrase against Adzuna; results are merged.
# ---------------------------------------------------------------------------
[KEYWORDS]
data scientist
data science
machine learning engineer
ML engineer
applied scientist
research scientist
data analyst


# ---------------------------------------------------------------------------
# EXCLUDE_TITLES — if a job's title contains any of these (as a whole
# word/phrase, case-insensitive), it's skipped entirely. One per line.
# Matching is exact (no wildcard) so it stays predictable — list inflected
# forms separately if needed (e.g. both "praktikant" and "praktikantin"
# are included below).
# ---------------------------------------------------------------------------
[EXCLUDE_TITLES]
praktikum
praktikant
praktikantin
werkstudent
werkstudentin
intern
internship
working student
duales studium
dual studium
dual study
software engineer
data engineer
professur
juniorprofessur
lehrstuhl


# ---------------------------------------------------------------------------
# EXCLUDE_COMPANIES — jobs from these companies are skipped entirely,
# regardless of how good a title/keyword match they are. One per line,
# whole word/phrase, case-insensitive. Matches against Adzuna's company
# display name for the listing.
# ---------------------------------------------------------------------------
[EXCLUDE_COMPANIES]
Smart Future Campus


# ---------------------------------------------------------------------------
# EXCLUDE_GERMAN_LEVELS — job is skipped if the LLM determines German is a
# MANDATORY requirement at one of these levels. One per line. Common
# values: b1, b2, c1, c2, fluent, native. Jobs where German is mentioned
# as optional/nice-to-have are never excluded, regardless of this list.
# ---------------------------------------------------------------------------
[EXCLUDE_GERMAN_LEVELS]
c1
c2
fluent
native


# ---------------------------------------------------------------------------
# LOCATIONS — where to search. Use "Germany" (exactly, capital G) for a
# nationwide search with no location filter. Add more lines for specific
# cities to also search (helps catch listings that fall outside the
# nationwide query's per-keyword result cutoff). One per line.
# ---------------------------------------------------------------------------
[LOCATIONS]
Germany
Berlin


# ---------------------------------------------------------------------------
# MIN_MATCH_SCORE — jobs scored below this (out of 10) by the LLM are
# skipped. Single number, 1-10. Jobs where scoring itself failed are
# still sent regardless of this setting.
# ---------------------------------------------------------------------------
[MIN_MATCH_SCORE]
4


# ---------------------------------------------------------------------------
# ADZUNA_COUNTRY — Adzuna's country code to search. "de" = Germany. See
# https://developer.adzuna.com/docs/search for the full list of codes if
# you want to search a different country instead (e.g. "gb", "us", "fr").
# ---------------------------------------------------------------------------
[ADZUNA_COUNTRY]
de


# ---------------------------------------------------------------------------
# GROQ_MODEL — which Groq-hosted model scores each job's match and
# extracts the German requirement / years of experience. Default is
# fast/cheap with high rate-limit headroom on Groq's free tier. If you're
# on a paid Groq tier, a larger model (e.g. "openai/gpt-oss-120b") may
# give more accurate scoring at the cost of tighter rate limits.
# ---------------------------------------------------------------------------
[GROQ_MODEL]
openai/gpt-oss-20b


# ---------------------------------------------------------------------------
# GROQ_CALL_PACING_SECONDS — seconds to wait between scoring calls, to
# stay within Groq's free-tier ~6,000-8,000 tokens/minute limit. Lower
# this if you're on a paid tier with a higher limit; raise it if you're
# still seeing rate-limit warnings in the logs.
# ---------------------------------------------------------------------------
[GROQ_CALL_PACING_SECONDS]
20


# ---------------------------------------------------------------------------
# MAX_JOBS_SCORED_PER_RUN — safety cap on how many jobs get scored in a
# single run, protecting Groq's free-tier 1,000-requests/DAY limit from
# being exhausted by one large backlog. Jobs beyond this cap still get
# sent, just without a score. Raise this if you're on a paid tier.
# ---------------------------------------------------------------------------
[MAX_JOBS_SCORED_PER_RUN]
60


# ---------------------------------------------------------------------------
# MAX_DESCRIPTION_CHARS — how much job-description text gets sent to Groq
# per scoring call, to bound token cost. Kept well above Adzuna's ~600
# char snippet size (for accuracy on requirements buried later in a
# posting) but capped to stay within the free-tier token/minute budget.
# Raise this if you're on a paid tier and want richer context per call.
# ---------------------------------------------------------------------------
[MAX_DESCRIPTION_CHARS]
1500
