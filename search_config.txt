# Job alert search configuration

# Edit this file to customize what the bot searches for and filters 
# After editing, commit and push this file — the next run picks it up
# automatically.
# =============================================================================

# ---------------------------------------------------------------------------
# KEYWORDS — job title/description phrases to search for (one per line).
# Each is queried as an exact phrase against Adzuna; results are merged.
# ---------------------------------------------------------------------------
[KEYWORDS]
data scientist
data science
machine learning engineer
ML engineer
applied scientist
research scientist
data analyst


# ---------------------------------------------------------------------------
# EXCLUDE_TITLES — if a job's title contains any of these (as a whole word/phrase, case-insensitive), it's skipped entirely. One per line.
# Matching is exact (no wildcard) so it stays predictable
# ---------------------------------------------------------------------------
[EXCLUDE_TITLES]
praktikum
praktikant
praktikantin
werkstudent
werkstudentin
intern
internship
working student
duales studium
dual studium
dual study
software engineer
data engineer


# ---------------------------------------------------------------------------
# EXCLUDE_GERMAN_LEVELS — job is skipped if the LLM determines German is a
# MANDATORY requirement at one of these levels. One per line. Common
# values: b1, b2, c1, c2, fluent, native. Jobs where German is mentioned
# as optional/nice-to-have are never excluded, regardless of this list.
# ---------------------------------------------------------------------------
[EXCLUDE_GERMAN_LEVELS]
c1
c2
fluent
native


# ---------------------------------------------------------------------------
# LOCATIONS — where to search. Use "Germany" (exactly, capital G) for a
# nationwide search with no location filter. Add more lines for specific
# cities to also search (helps catch listings that fall outside the
# nationwide query's per-keyword result cutoff). One per line.
# ---------------------------------------------------------------------------
[LOCATIONS]
Berlin
Germany



# ---------------------------------------------------------------------------
# MIN_MATCH_SCORE — jobs scored below this (out of 10) by the LLM are
# skipped. Single number, 1-10. Jobs where scoring itself failed are
# still sent regardless of this setting.
# ---------------------------------------------------------------------------
[MIN_MATCH_SCORE]
4


# ---------------------------------------------------------------------------
# ADZUNA_COUNTRY — Adzuna's country code to search. "de" = Germany. See
# https://developer.adzuna.com/docs/search for the full list of codes if
# you want to search a different country instead (e.g. "gb", "us", "fr").
# ---------------------------------------------------------------------------
[ADZUNA_COUNTRY]
de


# ---------------------------------------------------------------------------
# Groq model and search parameters
#
# NOTE: openai/gpt-oss-20b's free tier is 30 requests/min, 1,000 requests/
# DAY, 8,000 tokens/min - and that budget is SHARED with linkedin_alert.py,
# since both scripts use the same GROQ_API_KEY. Running this workflow very
# frequently (e.g. every 10 min = up to 144 runs/day) eats into that shared
# daily budget fast, especially alongside the LinkedIn workflow's own usage.
# If you see "Groq quota exhausted" warnings in the Action logs, the fix is
# a less frequent cron-job.org schedule for one or both workflows, not a
# bigger GROQ_CALL_PACING_SECONDS - that only paces requests within a single
# run, it can't stretch a 1,000/day cap that both scripts draw from.
# ---------------------------------------------------------------------------
[GROQ_MODEL]
openai/gpt-oss-20b


[GROQ_CALL_PACING_SECONDS]
15


[MAX_JOBS_SCORED_PER_RUN]
60

[MAX_DESCRIPTION_CHARS]
2000
